version: "3"

services:

  oncodash:
    user: $UID:$GID
    build:
      context: .
      dockerfile: ./Dockerfile.dev
    ports:
      - "8888:8888"
    volumes:
      - type: bind
        source: ./
        target: /opt/app/
    