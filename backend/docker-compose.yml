version: "3"

services:

  oncodash:
    user: $UID:$GID
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - 8000:8000
    volumes:
      - .:/opt/app/
    container_name: django_app
    command: > 
      sh -c "python manage.py runserver 0.0.0.0:8000"
    