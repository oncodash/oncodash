"""
The Explainer module merge various data in a single causal network for oncologists.
"""
import logging
logger = logging.getLogger("django.oncodash.explainer")
logger.debug("Initialize explainer")
